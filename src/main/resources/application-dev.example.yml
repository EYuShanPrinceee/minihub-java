spring:
  datasource:
    url: "jdbc:mysql://127.0.0.1:3306/minihub?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true"
    username: root
    password: "你的本地数据库密码"
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      timeout: 3000ms

app:
  jwt:
    secret: "请替换成至少 32 字节的开发环境密钥"